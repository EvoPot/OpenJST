import 'package:flutter/material.dart';

//These are the only colors that the app calls for its menus. feel free to edit them

class appColors {
  final Color secondaryAccentColor = Color.fromARGB(255, 199, 104, 236);
  final Color mainAccentColor = Color.fromARGB(255, 82, 226, 195);
  final Color darkGrey = Color.fromARGB(255, 31, 31, 31);
  final Color slightlyDarkerGrey = Color.fromARGB(255, 17, 17, 17);
}
